<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title></title>
    @Styles.Render("~/Content/css")
    @Scripts.Render("~/bundles/modernizr")

</head>
<body style="background-color:gray">
    <div class="navbar navbar-inverse navbar-fixed-top">

        <div class="container">
            <div class="navbar-header">
                <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                </button>
                @Html.ActionLink("Palindrom", "Index", "Home", new { area = "" }, new { @class = "navbar-brand", @style = "font-size: 15pt;" })
            </div>
            <div class="navbar-collapse collapse">
                <ul class="nav navbar-nav">


                    @if (User.IsInRole("admin") || User.IsInRole("urednik"))
                    {
                        <li>@Html.ActionLink("Dodaj", "DodajSadrzaj", "Home")</li>

                    }

                </ul>
                @Html.Partial("_LoginPartial")
            </div>
        </div>
    </div>


   @* @if (!HttpContext.Current.Request.RawUrl.Contains("UcitajTemu"))
    {
        @Html.Partial("StranaDelimicni");

    }
    
    @if (HttpContext.Current.Request.RawUrl.Contains("Index"))
    {
    @Html.Partial("DelimicniKorisniciAdoZ");

    }
       *@

    <div class="container body-content" style="height:max-content;background-color:gray">
        @RenderBody()

        <footer>
        </footer>
    </div>

    @Scripts.Render("~/bundles/jquery")
    @Scripts.Render("~/bundles/bootstrap")
    @RenderSection("scripts", required: false)

    <style>
        * {
            font-size: 15pt;
        }
    </style>
</body>
</html>
